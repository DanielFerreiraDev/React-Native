/**
 * @format
 */

import {AppRegistry} from 'react-native';
import mainNavigation from './src2/route'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => mainNavigation);
